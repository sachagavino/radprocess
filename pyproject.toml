[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "radprocess"
description = "A package that runs postprocessing of MHD simulations using multi-grain models."
readme = "README.md"
authors = [{name = "Sacha Gavino", email = "sacha.gavino@unibo.it"}]
dynamic = ["version"]